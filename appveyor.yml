build: off


init:
- cmd: ECHO Starting ZUT.PokerPlanning app building
- cmd: dir
- cmd: ./ZUT-PokerPlanningClient

install:
- ps: Install-Product node $env:nodejs_version
- npm install
  
test_script:
- node --version
- npm --version
- ng --version
- ng build 
- npm start
  
  
